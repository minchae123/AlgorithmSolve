#include <iostream>
#include <vector>
int dp[1000001] = { 0,1,2,3,1,1 };
using namespace std;
int main()
{
	//int n;
	//cin >> n;

	//for (int i = 6; i <= n; i++)
	//{
	//	dp[i] = min(dp[i - 1] + 1, min(dp[i - 4] + 1, dp[i - 5] + 1));
	//}

	//cout << dp[n];
}